#!/usr/bin/env bash

# It uses FZF to preview the constants and get info about them like who discovered, name, use, value (in both numbers and in terms of other constants).
# It contains square roots, trigonometric tables, important formulae etc.
